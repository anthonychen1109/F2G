<div class="header container">

  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#"><div class="header-left">
    <%= image_tag "flatiron.png" %>
    <h2>Flat-a-gram</h2>
  </div></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item active">
        </li>
          <% if logged_in? %>
        <div
          class="logout-button">
          <a class="logout-button" href="#"> <%= link_to "UserPage", user_path(@user)%> <span class="sr-only">(current)</span></a>
          <a class="logout-button" href="#"> <%= link_to "CreatePost", new_post_path%> <span class="sr-only">(current)</span></a>
          <a class="logout-button" href="#"> <%= link_to "PostFeed", posts_path%> <span class="sr-only">(current)</span></a>

          <%= link_to "Logout", logout_path, method: :delete %>
        </div>
    <% end %>
      </ul>
    </div>
  </nav>

  </div>
  <hr />


<div class="container-fluid">

  <%= form_for @comment do |f| %>
  <%= f.hidden_field :content %> <p>
  <%= f.text_area :content, cols: "45", rows: "2", placeholder: "Add a comment..." %><p>
  <%= f.hidden_field :post_id %>
  <%= f.hidden_field :user_id %>
  <%= f.submit "Post", class: 'btn btn-primary' %><p>
  <% end  %>
</div>
